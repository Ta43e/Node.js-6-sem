BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[subscribers] (
    [chat_id] BIGINT NOT NULL,
    CONSTRAINT [PK_subscribers] PRIMARY KEY CLUSTERED ([chat_id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
